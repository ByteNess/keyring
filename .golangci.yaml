version: "2"
linters:
  settings:
    depguard:
      rules:
        main:
          list-mode: lax
          files:
            - $all
          allow:
            - $gostd
  exclusions:
    rules:
      - path: _test\.go
        linters: [dupl, revive, errcheck, nilnil]
      # Renaming the existing 1Password error vars (e.g. OPErrClient ->
      # ErrOPClient) would break the public API, so keep the current names.
      - linters: [revive, errname, staticcheck]
        text: 'OP\w*Err\w+'
      - linters: [dupl]
        path: pass(age)?\.go
  enable:
    - bodyclose
    - contextcheck
    - depguard
    - durationcheck
    - dupl
    - errchkjson
    - errname
    - exhaustive
    - gocritic
    - makezero
    - misspell
    - nakedret
    - nilerr
    - nilnil
    - noctx
    - govet
    - predeclared
    - prealloc
    - revive
    - thelper
    - tparallel
    - unconvert
    - unparam
    - whitespace
formatters:
  enable:
    - gofmt
    - goimports
